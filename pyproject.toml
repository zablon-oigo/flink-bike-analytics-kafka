[project]
name = "notifications"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.42.30",
    "fastapi[standard]>=0.128.0",
    "httpie>=3.2.4",
    "pydantic>=2.12.5",
]
